#ifndef _PREWITT_H
	#define _PREWITT_H

		#include <math.h>
		#include <float.h>
		#include <stdio.h>
		#include <stdint.h>
		#include <stdlib.h>
		#include "bitmap.h"

	void Prewitt_Filter(bitmap_t *, int, int);

#endif
